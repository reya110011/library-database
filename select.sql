-- to select all columns from the `books` table
select *from select *from books;

-- select only `title` column from the `books` table
select title from books;

-- select rows based on `publicationyear`
select
    *

frombooks
where
publication year=2018